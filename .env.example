# Server-only CallTrackingMetrics FormReactor credentials for /api/lead/.
# Never prefix these with PUBLIC_: they must not ship to the browser.
# Get the Sunview reactor from CTM (account 362771) > Forms > FormReactor.
CTM_FORMREACTOR_ENDPOINT=https://api.calltrackingmetrics.com/api/v1/formreactor/YOUR_REACTOR_ID
CTM_FORMREACTOR_KEY=replace-me
